import { sorafiTitle } from "@/style/template";
const Logo = () => {
  return (
    <div className="flex flex-row">
      <button className={`${sorafiTitle} px-20`}>Sorafi</button>
    </div>
  );
};

export default Logo;
